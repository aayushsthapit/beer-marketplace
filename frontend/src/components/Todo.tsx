import React from 'react';

// Component for Todo.
function Todo() {
    const [todos, setTodos] = React.useState(null);

    React.useEffect(() => {

    },[]);

    return <div>Hello world</div>
}

export default Todo;
